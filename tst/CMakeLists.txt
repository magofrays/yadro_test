find_package(GTest)
if(GTest_FOUND)
    enable_testing()
    add_executable(tst club_test.cpp main.cpp)
    target_link_libraries(tst PRIVATE ClubSystemLib gtest gtest_main)
    add_test(NAME tst COMMAND tst)
else()
    message(STATUS "GTests not found, not building tests")
endif()